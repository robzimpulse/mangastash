import 'package:flutter/material.dart';

class MangaTileWidget extends StatelessWidget {
  const MangaTileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('🚧🚧🚧 Under Construction 🚧🚧🚧'),
    );
  }

}