name: core_network
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.7.12"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  dio_smart_retry: ^6.0.0
  system_proxy: ^0.1.0
  url_launcher: ^6.1.11
  mutex: ^3.1.0

  # library
  service_locator:
    path: ../../library/service_locator
  dio_inspector:
    path: ../../library/dio_inspector

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
