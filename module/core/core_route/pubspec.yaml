name: core_route
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.19.6 <4.0.0"
  flutter: ">=3.7.12"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^7.0.0

  # library
  service_locator:
    path: ../../library/service_locator

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
