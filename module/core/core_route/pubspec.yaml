name: core_route
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.7.2 <4.0.0"
  flutter: ">=3.29.3"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  rxdart: ^0.27.7
  log_box:
    git:
      url: https://github.com/robzimpulse/log_box.git
      ref: master
      path: packages/log_box

  # library
  service_locator:
    path: ../../library/service_locator

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
