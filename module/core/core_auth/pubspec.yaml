name: core_auth
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.7.12"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.7
  equatable: ^2.0.5

  # library
  service_locator:
    path: ../../library/service_locator

  # core
  core_storage:
    path: ../../core/core_storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
