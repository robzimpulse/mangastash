name: domain_manga
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.19.6 <4.0.0"
  flutter: ">=3.7.12"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.16.0
  rxdart: ^0.27.7

  # core
  core_network:
    path: ../../core/core_network

  # data
  data_manga:
    path: ../../data/data_manga

  # entity
  entity_manga:
    path: ../../entity/entity_manga

  # library
  service_locator:
    path: ../../library/service_locator
  manga_dex_api:
    path: ../../library/manga_dex_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
