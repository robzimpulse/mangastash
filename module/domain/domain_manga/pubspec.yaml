name: domain_manga
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.6.2 <4.0.0"
  flutter: ">=3.27.4"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.16.0
  rxdart: ^0.27.7
  background_downloader: ^8.7.1
  flutter_inappwebview: ^6.1.0
  html: ^0.15.5
  queue: ^3.4.0
  universal_io: ^2.2.2

  # core
  core_network:
    path: ../../core/core_network
  core_environment:
    path: ../../core/core_environment
  core_auth:
    path: ../../core/core_auth
  core_storage:
    path: ../../core/core_storage

  # entity
  entity_manga:
    path: ../../entity/entity_manga

  # library
  service_locator:
    path: ../../library/service_locator
  manga_dex_api:
    path: ../../library/manga_dex_api
  manga_service_firebase:
    path: ../../library/manga_service_firebase
  log_box:
    path: ../../library/log_box

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
