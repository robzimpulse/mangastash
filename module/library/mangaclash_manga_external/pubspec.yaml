name: mangaclash_manga_external
description: "A new Flutter project."
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.7.2 <4.0.0"
  flutter: ">=3.29.3"

dependencies:
  flutter:
    sdk: flutter

  dart_eval: ^0.8.3
  eval_annotation: ^0.8.1

  manga_dex_api:
    path: ../manga_dex_api
  entity_manga_external:
    path: ../entity_manga_external

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0